---
phase: 03-settings-history
plan: 05
type: execute
wave: 4
depends_on: [03-02, 03-03, 03-04]
files_modified: []
autonomous: false
requirements: [APP-04, DISC-02, DISC-03, HIST-01, HIST-02]

must_haves:
  truths:
    - "Settings window opens from menu bar click and Cmd+,"
    - "General tab: auto-backup toggle, retention stepper, and launch-at-login toggle all function correctly"
    - "Watch Folders tab: existing watch folders visible; + adds a new folder; - with confirmation removes it"
    - "Destinations tab: shows the configured destination"
    - "History tab: after a backup run, a project appears in the left panel; selecting it shows version history on the right"
    - "History tab: version rows show destination icon and verified/corrupt status"
    - "About tab: shows app name and version"
  artifacts:
    - path: "AbletonBackup/Views/Settings/SettingsView.swift"
      provides: "Verified 5-tab settings window"
      contains: "TabView"
  key_links: []
---

<objective>
Human verification that all Phase 3 success criteria are met end-to-end. The app is running and the user confirms each Settings tab functions correctly.

Purpose: Functional verification of the complete settings and history experience. All five tabs must be confirmed working before Phase 3 is complete.
Output: Phase 3 verified complete.
</objective>

<execution_context>
@/Users/eric/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eric/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-settings-history/03-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and launch app for verification</name>
  <files></files>
  <action>
Build the app and confirm it launches successfully:

```bash
cd /Users/eric/dev/CC
xcodebuild -scheme AbletonBackup -destination 'platform=macOS' build 2>&1 | tail -10
```

If BUILD SUCCEEDED, the app can be launched from Xcode or the build products directory. Confirm the build output shows no warnings about Swift 6 concurrency violations.

Prepare the verification checklist for the human checkpoint.
  </action>
  <verify>
    <automated>cd /Users/eric/dev/CC && xcodebuild -scheme AbletonBackup -destination 'platform=macOS' build 2>&1 | grep -E "BUILD (SUCCEEDED|FAILED)"</automated>
  </verify>
  <done>BUILD SUCCEEDED confirmed. App ready for manual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 3 tabs and features</name>
  <files></files>
  <action>
Pause for user to launch AbletonBackup and verify all five Settings tabs function correctly per the checklist in how-to-verify. Resume only after user types "approved" or reports issues.
  </action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; verification is performed manually by the user</automated>
  </verify>
  <done>User confirms all Phase 3 success criteria are met by typing "approved".</done>
  <what-built>
Complete Phase 3 Settings + History implementation:
- Settings scene with 5 tabs (General, Watch Folders, Destinations, History, About)
- General: auto-backup toggle, retention stepper, Launch at Login
- Watch Folders: add via NSOpenPanel, remove with confirmation sheet
- Destinations: read-only list of configured destinations
- History: two-panel NavigationSplitView with per-project version history and destination indicators
- About: app name and version
  </what-built>
  <how-to-verify>
Launch AbletonBackup from Xcode or build products. Then verify each item:

1. **Settings opens:**
   - Click the menu bar icon -> click "Settings..." -> Settings window should open
   - Press Cmd+, -> Settings window should open or come to front
   - Expected: Native macOS tabbed settings window with 5 tabs

2. **General tab:**
   - Toggle "Auto-backup on save" -- should persist across app restarts (check UserDefaults key "autoBackupEnabled")
   - Adjust the retention stepper up/down -- value should change and persist
   - "Launch at Login" toggle should reflect current state

3. **Watch Folders tab:**
   - Existing watch folder (auto-discovered Ableton Projects folder) should appear in the list
   - Click + -> NSOpenPanel folder picker should open
   - Select any folder -> it should appear in the list
   - Select the newly added folder -> click - -> confirmation sheet should appear saying "Stop watching 'X'? Existing backups are not affected."
   - Confirm removal -> folder should disappear from list

4. **Destinations tab:**
   - Should show "App Support Backup" (the bootstrap destination from Phase 2)
   - No add/remove controls present (Phase 3 is read-only)

5. **History tab:**
   - If no backups have run: left panel shows "No backup history yet" message
   - If a backup has run (trigger one via "Back Up Now" if needed):
     - Left panel shows project name with relative last-backup time
     - Selecting the project shows version rows on the right
     - Each row shows: timestamp, destination icon (externaldrive.fill for local), green checkmark for verified
   - Corrupt versions (if any): red text and warning icon

6. **About tab:**
   - Shows "AbletonBackup" title and version number

Report any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" if all items verified, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All Phase 3 success criteria confirmed by user:
1. Settings window covers all configuration (APP-04)
2. Watch folder can be added (DISC-02)
3. Watch folder can be removed (DISC-03)
4. Version history visible per project (HIST-01)
5. Version history shows destinations and status (HIST-02)
</verification>

<success_criteria>
User types "approved" confirming all five tabs function correctly. Phase 3 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-settings-history/03-05-SUMMARY.md`
</output>
