---
phase: 01-backup-engine
task: 2
total_tasks: 5
status: mid_wave_2
last_updated: 2026-02-25T22:00:00.000Z
---

<current_state>
Wave 1 complete (01-01, 01-02 done). Wave 2 partially blocked — both 01-03 and 01-04 background agents had Write/Bash permissions denied. Plans have been read and are ready to execute directly in-session. No Wave 2 files have been created yet. The next action is to execute 01-03 (FileCopyPipeline) and 01-04 (VersionManager TDD) directly.
</current_state>

<completed_work>

Wave 1 — COMPLETE:
- 01-01: Swift package + GRDB 7.10.0 + xxHash-Swift 1.1.1 schema, all model types, 6 tests passing
  Commits: 690c743, 80cfd34, 51bf4dd
- 01-02: ProjectResolver TDD (17 tests passing), BackupJob contracts
  Commits: f27b22f, 789c534

Wave 2 — BLOCKED (agents denied permissions, need direct execution):
- 01-03: NOT started — plan read, ready to implement
- 01-04: NOT started — plan read, ready to implement

</completed_work>

<remaining_work>

IMMEDIATE (this session):
1. Execute 01-03 directly:
   - Create Sources/BackupEngine/Destinations/DestinationAdapter.swift (protocol + TransferProgress + DestinationStatus)
   - Create Sources/BackupEngine/FileCopyPipeline.swift (COPYFILE_CLONE + CryptoKit SHA256 inline checksum; xxHash-Swift also available)
   - Create Tests/BackupEngineTests/FileCopyPipelineTests.swift (5 tests)
   - Create Sources/BackupEngine/Destinations/LocalDestinationAdapter.swift
   - Create Tests/BackupEngineTests/LocalDestinationAdapterTests.swift (4 tests)
   - Run swift test, commit atomically per task
   - Write 01-03-SUMMARY.md

2. Execute 01-04 directly:
   - Create Sources/BackupEngine/BackupManifest.swift (BackupManifest + ManifestEntry)
   - Create Tests/BackupEngineTests/VersionManagerTests.swift (9 failing tests — RED)
   - Create Sources/BackupEngine/VersionManager.swift (GREEN implementation)
   - Run swift test --filter VersionManagerTests, commit atomically per task
   - Write 01-04-SUMMARY.md

3. Then execute Wave 3: 01-05 (BackupEngine actor + integration tests)

</remaining_work>

<key_context>

CRITICAL facts for 01-03 implementation:
- xxHash-Swift 1.1.1 IS available (confirmed in 01-01-SUMMARY.md) — use it for checksums
- xxHash API: XXH64 streaming — update(_ data: Data) + digestHex()
- BackupFileRecord.sourceSize is Int (not Int64) — confirmed in actual file
- FileEntry.size is Int64 — confirmed in ProjectResolver.swift
- Use COPYFILE_CLONE (not COPYFILE_CLONE_FORCE) — ExFAT fallback preserved
- After APFS clone (result == 0): compute checksum from destination file (one read is fine, clone is instant)
- After chunked copy failure: inline hashing during copy loop (zero extra I/O)
- Version directory structure: {rootPath}/{versionID}/{relativePath}

CRITICAL facts for 01-04 implementation:
- VersionStatus is defined in Sources/BackupEngine/Persistence/Models/VersionStatus.swift
- AppDatabase.makeInMemory() uses UUID temp file (WAL incompatible with :memory:)
- Version ID format: "yyyy-MM-dd'T'HHmmss.SSS-{uuid-prefix-8-lowercase}"
  Example: "2026-02-25T143022.456-a3f8b12c"
- Pruning rules: only .verified counts toward retention; .corrupt excluded; locked skipped
- Write-then-cleanup: mark status=.deleting in DB transaction BEFORE disk deletion

Existing files NOT to modify:
- Sources/BackupEngine/BackupJob.swift (has BackupJob, BackupJobResult, DestinationResult)
- Sources/BackupEngine/BackupEngine.swift (stub — wait for 01-05)
- All Persistence/ files

</key_context>

<next_action>
/clear then resume with: "Continue executing Wave 2 plans 01-03 and 01-04 for phase 01-backup-engine. Read .continue-here.md first."

Execute 01-03 first (DestinationAdapter protocol + FileCopyPipeline + LocalDestinationAdapter), then 01-04 (BackupManifest + VersionManager TDD), then Wave 3 plan 01-05.
</next_action>
